# Configuration du registre Docker
# Ce fichier peut être source pour définir les variables d'environnement

# Docker Hub Configuration
DOCKER_HUB_USERNAME=mocherqu
DOCKER_HUB_IMAGE_NAME=devtechly
DOCKER_HUB_TAG=latest

# GitHub Container Registry (optionnel)
# GITHUB_USERNAME=votre-username-github
# GITHUB_TOKEN=votre-token-github

# Registre privé (optionnel)
# PRIVATE_REGISTRY=registry.example.com
# PRIVATE_REGISTRY_USERNAME=votre-username

# Pour utiliser ces variables dans PowerShell:
# Get-Content docker-registry-config.env | ForEach-Object { if ($_ -match '^([^#][^=]*)=(.*)$') { [Environment]::SetEnvironmentVariable($matches[1], $matches[2], "Process") } }

# Pour utiliser dans Bash:
# export $(grep -v '^#' docker-registry-config.env | xargs)
